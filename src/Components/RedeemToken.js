import React from 'react';
import { Card } from 'react-bootstrap';

const RedeemToken = () => (
    <Card>
        <Card.Header>Redeem Token</Card.Header>
        <Card.Body></Card.Body>
    </Card>
);

export default RedeemToken;
